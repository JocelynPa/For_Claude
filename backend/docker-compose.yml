services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tesla_companion
    ports:
      - "5432:5432"
    volumes:
      - tesla_companion_db:/var/lib/postgresql/data

volumes:
  tesla_companion_db:
