services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: tesla_companion
      POSTGRES_PASSWORD: tesla_companion
      POSTGRES_DB: tesla_companion
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
